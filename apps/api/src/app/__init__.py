"""API host package. The composition root: it pulls in the nucleus, the modules,
and the jurisdiction plugin, then (from B10) discovers → toposorts → migrates →
mounts them. Owns no domain logic of its own.
"""
