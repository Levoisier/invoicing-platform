import type { NextConfig } from "next";

// Nothing custom yet: the app is a plain client-side SPA that calls the API
// directly (NEXT_PUBLIC_API_URL), so no rewrites/proxy are needed.
const nextConfig: NextConfig = {};

export default nextConfig;
